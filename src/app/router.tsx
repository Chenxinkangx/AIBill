// Central reference for route paths.
// Routes are defined inline in App.tsx for simplicity in Phase 1.
export const ROUTES = {
  HOME: '/',
  ADD_RECORD: '/add',
  RECORDS: '/records',
  BUDGET: '/budget',
  SETTINGS: '/settings',
} as const
